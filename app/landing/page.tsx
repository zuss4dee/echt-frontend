import LandingPage from "@/components/marketing/LandingPage";

/** Canonical marketing UI also lives at `/`; this route stays for bookmarks and old links. */
export default function LandingRoute() {
  return <LandingPage />;
}
